import { describe, expect, it } from "vitest";
import {
  breadcrumbScript,
  itemListScript,
} from "../apps/web/src/lib/seo-jsonld-lib";

describe("seo-jsonld-lib", () => {
  it("builds breadcrumb script", () => {
    expect(breadcrumbScript([{ name: "Home", path: "/" }]).type).toBe(
      "application/ld+json",
    );
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
  it("itemListScript matrix 0", () => {
    const script = itemListScript([{ name: "Item 0", path: "/mcp/item-0" }], {
      name: "List 0",
    });
    expect(script.children).toContain("Item 0");
  });
});
